import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xff3b544b);
  static const Color onPrimary = Colors.white;

  static const Color secondary = Color(0xff0d291e);
  static const Color onSecondary = Colors.white;

  static const Color background = secondary;
  static const Color onBackground = primary;

  static const Color surface = primary;
  static const Color onSurface = Colors.white;

  static const Color error = Colors.red;
  static const Color onError = Colors.white;

  static const Color accent = Color(0xff73f8b7);
  static const Color textSpecialColor = accent;
  static const Color textColor = Colors.white;
}
